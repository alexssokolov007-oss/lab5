# API

## CLI

```
python -m src.main [--numbers N [N ...]] [--file PATH] [--normalize]
```

### Опции

- `--numbers`: список чисел из командной строки
- `--file`: путь к файлу с числами, разделенными пробелами или запятыми
- `--normalize`: добавить нормализованные значения в отчет
